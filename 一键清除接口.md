接口概述
通信协议：HTTP
请求方式：POST
接口地址：http://C2ip:8080/execute
请求参数说明
共有三个状态，清除，查询，重置。默认是不清除状态，值为0。当执行清除功能时，值会被置为1。重置接口会重置回不清除状态。
ramnit：
	清除接口："/admin/ramnit/cleanup"
	查询接口："/admin/ramnit/status"
	重置接口："/admin/ramnit/reset"
autoupdate：
	清除接口："/admin/ autoupdate/cleanup"
	查询接口："/admin/ autoupdate/status"
	重置接口："/admin/ autoupdate/reset"
irc(该程序默认清除后会重置回正常模式)：
	清除接口："/admin/ irc/cleanup"
	查询接口："/admin/ irc/status"
	重置接口："/admin/ irc/reset"

Auth-token：3@UNyeExV9HzHeJ!9HG$k4v3FaefwU2RZ%DbgX6wFSTT3^&YqjG&X#*HfT7Y4S5n

X-Safety-Code：
Mrcm3YsTNFyJQ685m@bL&nhm!8jyaP&sw9@qz^BJMKkqHh@rzV5GEptkxq9@3Z5e
代码集成示例
import requests
import urllib3
urllib3.disable_warnings(urllib3.exceptions.InsecureRequestWarning)

BASE_URL = "https://172.16.88.36:443"
AUTH_TOKEN = "3@UNyeExV9HzHeJ!9HG$k4v3FaefwU2RZ%DbgX6wFSTT3^&YqjG&X#*HfT7Y4S5n"
SAFETY_CODE = "Mrcm3YsTNFyJQ685m@bL&nhm!8jyaP&sw9@qz^BJMKkqHh@rzV5GEptkxq9@3Z5e"

headers = {
    "X-Auth-Token": AUTH_TOKEN,
    "X-Safety-Code": SAFETY_CODE
}

# 查询状态
resp = requests.post(f"{BASE_URL}/admin/ramnit/status", headers=headers, verify=False)
print(resp.json())

# 开启清除模式
resp = requests.post(f"{BASE_URL}/admin/ramnit/cleanup", headers=headers, verify=False)
print(resp.json())

# 查询状态
resp = requests.post(f"{BASE_URL}/admin/ramnit/status", headers=headers, verify=False)
print(resp.json())

# 重置为日志模式
resp = requests.post(f"{BASE_URL}/admin/ramnit/reset", headers=headers, verify=False)
print(resp.json())

# 查询状态
resp = requests.post(f"{BASE_URL}/admin/ramnit/status", headers=headers, verify=False)
print(resp.json())

终端输出：
{'success': True, 'mode': 'logging', 'cleanup_mode': 0}
{'success': True, 'message': 'Cleanup mode activated'}
{'success': True, 'mode': 'cleanup', 'cleanup_mode': 1}
{'success': True, 'message': 'Reset to logging mode'}
{'success': True, 'mode': 'logging', 'cleanup_mode': 0}

注意：请勿将 auth_code和safe_code 硬编码在前端 JavaScript 代码中，务必仅在后端服务器发起请求。
